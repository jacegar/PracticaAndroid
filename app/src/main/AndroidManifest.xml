<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticaAndroid">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RutinaActivity"
            android:exported="false"
            android:label="Gestión de Rutinas" />
        <activity
            android:name=".SesionActivity"
            android:exported="false"
            android:label="Gestión de Sesiones" />
        <activity
            android:name=".SesionEjerciciosActivity"
            android:exported="false"
            android:label="Ejercicios de la Sesión" />
    </application>

</manifest>